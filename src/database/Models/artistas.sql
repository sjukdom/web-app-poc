CREATE TABLE artista (
   id INT NOT NULL AUTO_INCREMENT,
   nombre VARCHAR(100) NOT NULL,
   rfc VARCHAR(50),
   direccion VARCHAR(100),
   telefono VARCHAR(20),
   email VARCHAR(100),
   PRIMARY KEY (id)
);

CREATE TABLE album (
   id
   nombre VARCHAR(50),
   precio INTEGER
)